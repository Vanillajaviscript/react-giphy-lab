// import {useState} from "react";


const Button = ({gif}) => {
const {data} = gif;
  const {embed_url, title} = gif;
    return (
        <div className="form">
           
        </div>
    )
}

export default Button;